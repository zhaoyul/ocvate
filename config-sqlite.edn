;; =============================================================================
;; 企业资产图表分析 - SQLite 测试配置
;; =============================================================================
;; 使用方式: clojure -J-Dconf=config-sqlite.edn -M -m ocvate.core
;; 自动建表+示例数据，无需手动准备数据库。
;; =============================================================================

{:db {:dbtype "sqlite"
      :dbname "data/test.db"}

 ;; 视图名配置（仅 Oracle 生效，SQLite 固定使用默认表名）
 :views {}

 :auth {:enabled true
        :access-key "123"
        :session-ttl-ms 10800000}

 :server {:port 8080}}
